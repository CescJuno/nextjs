import Router from 'next/router';

const About = (props) => {

    return (<>
        <div> aaaa</div>
        <div>
            <button onClick={() => Router.push('/')}>Home</button>
        </div>
    </>)
};

export default About;
